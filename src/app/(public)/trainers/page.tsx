import TrainerPage from "@/components/TrainersPage";
import { trainersMetadata } from "@/metadata";

export const metadata = trainersMetadata;

const TrainersPage = () => {
  return <TrainerPage />;
};

export default TrainersPage;
